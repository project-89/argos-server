# MongoDB Connection Settings
MONGODB_URI=mongodb://argosUser:password@localhost:27017/argosDB
MONGODB_DATABASE=argosDB

# Firebase Settings (to be removed after migration)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_API_KEY=your-api-key

# Application Settings
PORT=3000
NODE_ENV=development

# Authentication
JWT_SECRET=your-jwt-secret
INTERNAL_SERVICE_TOKEN=your-internal-service-token

# Optional: MongoDB Atlas Connection (for production)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/argosDB?retryWrites=true&w=majority 